---
import Logo from "./Logo.astro";
import { Icon } from "astro-icon/components";
import { url } from "@src/utils/path";

export interface FooterType {
  description: string;
  links: { label: string; href: string }[];
  pages: { label: string; href: string }[];
  contact: {
    email: { label: string; href: string };
    phone: { label: string; href: string };
    address: string;
  };
  social: { icon: string; href: string }[];
}

const footerData: FooterType = {
  description:
    "Pioneering AI-driven solutions to transform industries with intelligent automation and advanced analytics since 2021.",
  links: [
    { label: "Home", href: "/" },
    { label: "About", href: "/about" },
    { label: "Projects", href: "/projects" },
    { label: "Contact", href: "/contact" },
  ],
  pages: [
    { label: "Privacy", href: "/privacy" },
    { label: "Terms", href: "/terms" },
    { label: "Blogs", href: "/blogs" },
    { label: "Docs", href: "/docs" },
  ],
  contact: {
    email: { label: "info@futura.ai", href: "#" },
    phone: { label: "+1 (800) 555-1234", href: "tel:+1-800-555-1234" },
    address: "123 AI Innovation St, San Francisco, CA",
  },
  social: [
    { icon: "fa6-brands:github", href: "https://github.com/Futura" },
    { icon: "fa6-brands:linkedin", href: "https://linkedin.com/company/Futura" },
    { icon: "fa6-brands:twitter", href: "https://twitter.com/Futura" },
  ],
};
---

<footer
  class="z-10 bg-cyber-dark py-16 relative border-t border-cyber-blue/20 overflow-hidden"
>
  <div
    class="absolute w-96 h-96 bg-cyber-blue/5 rounded-full blur-3xl -top-20 -left-20 animate-pulse-slow"
  ></div>
  <div
    class="absolute w-64 h-64 bg-cyber-blue/5 rounded-full blur-3xl -bottom-20 -right-20 animate-pulse-slow"
    style="animation-delay: 2s;"
  ></div>

  <div class="max-w-7xl mx-auto px-6 relative">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12">
      <div>
        <div class="mb-6">
          <Logo />
        </div>
        <p class="text-gray-300">{footerData.description}</p>
      </div>

      <div class="relative grid grid-cols-2 gap-4 lg:ps-12">
        <div>
          <h3 class="text-xl font-bold text-cyber-blue mb-6">Links</h3>
          <ul class="space-y-3">
            {footerData.links.map((link) => (
              <li>
                <a
                  href={url(link.href)}
                  class="text-gray-300 hover:text-cyber-blue transition-colors"
                >
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
        <div>
          <h3 class="text-xl font-bold text-cyber-blue mb-6">Pages</h3>
          <ul class="space-y-3">
            {footerData.pages.map((page) => (
              <li>
                <a
                  href={url(page.href)}
                  class="text-gray-300 hover:text-cyber-blue transition-colors"
                >
                  {page.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      </div>

      <div>
        <div class="mb-4">
          <h3 class="text-xl font-bold text-cyber-blue mb-6">Contact Us</h3>
          <ul class="space-y-3 text-gray-300">
            <li class="flex items-center">
              <Icon name="fa6-solid:envelope" class="text-cyber-blue mr-3" />
              <a
                href={footerData.contact.email.href}
                class="hover:text-cyber-blue transition-colors"
              >
                {footerData.contact.email.label}
              </a>
            </li>
            <li class="flex items-center">
              <Icon name="fa6-solid:phone" class="text-cyber-blue mr-3" />
              <a
                href={footerData.contact.phone.href}
                class="hover:text-cyber-blue transition-colors"
              >
                {footerData.contact.phone.label}
              </a>
            </li>
            <li class="flex items-start">
              <Icon name="fa6-solid:location-dot" class="text-cyber-blue mr-3 mt-1" />
              <span>{footerData.contact.address}</span>
            </li>
          </ul>
        </div>

        <div class="flex gap-6">
          {footerData.social.map((social) => (
            <a
              href={social.href}
              target="_blank"
              class="text-cyber-blue hover:text-white transition-colors"
            >
              <Icon name={social.icon} class="text-2xl" />
            </a>
          ))}
        </div>
      </div>
    </div>

    <div class="mt-12 pt-8 border-t border-cyber-blue/20 text-center">
      <p class="text-gray-400">
        © {new Date().getFullYear()} Futura AI Solutions. All rights reserved.
      </p>
    </div>
  </div>
</footer>